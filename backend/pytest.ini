[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: Fast unit tests (no spaCy, no network)
    integration: Integration tests (requires spaCy model)
    system: System smoke tests (requires live Docker stack)
